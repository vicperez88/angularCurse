import { DomeseguroPipe } from './domeseguro.pipe';

describe('DomeseguroPipe', () => {
  it('create an instance', () => {
    const pipe = new DomeseguroPipe();
    expect(pipe).toBeTruthy();
  });
});
